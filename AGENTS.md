# AIWorkFlow 根项目规则

1. 每次与用户对话都必须称呼用户为“超级无敌帅超超总”。
2. `AIWorkFlow` 是唯一 Git 根项目；仓库边界必须是本文件所在目录。
3. 唯一 GitHub 仓库是 `git@github.com:CloudyStormer/AI-Workflow-Dev.git`。本机可以使用解析到同一 GitHub 仓库的 SSH Host 别名，但不得创建第二个远端仓库。
4. `projects/` 下所有现在和未来的子项目，以及 `control-center/`，都是根仓中的普通目录；禁止在其中执行 `git init`、保留嵌套 `.git`、配置独立 remote、创建 submodule 或单独推送。
5. 提交或推送前必须运行 `scripts/check-git-boundary.sh`，并确认 `git rev-parse --show-toplevel` 指向 `AIWorkFlow` 根目录。
6. 依赖、构建缓存、环境变量、凭证、本机数据库、`.DS_Store` 和任何 `.git` 元数据不得提交。
7. 角色 Agent 永久复用现有 `00 包工头` 与 `01` 至 `11` 固定任务；新增项目不得复制角色任务。
8. 产品逻辑或 UI/UX 变更进入下游后，严格执行“产品独立交付并审核 → UI/UX 独立交付并审核 → 开发重新获批”的回退门；不得由开发越级代改。
9. 超级无敌帅超超总对当前明确交付回复“通过”时，同时批准该交付并授权唯一明确、输入完整且非高风险的下一站立即入场，无需再等“继续”。一次最多自动前进一步，下一站交付后必须重新停在审核门，禁止自动跑完整链路。
10. 下一站不唯一，或涉及生产发布、删除或不可逆覆盖、付费采购、账号权限、隐私数据、对外发送等高风险动作时，只能自动进入方案准备，不得执行实质动作，仍须单独明确授权。
11. 每次面向用户的回复，第一行必须先写项目模块标题：单项目使用 `【{项目显示名称} 项目】`；多项目按项目分别成块并重复标题；跨项目治理、角色协调或根仓事项单列 `【AIWorkFlow 总体协调】`。项目标题之后再使用完整称呼“超级无敌帅超超总”。
12. 阶段状态必须由对应固定角色本人报告：当前角色完成交付后，在自己的固定任务产生独立用户可见交付；当前产物“通过”并路由后，下一角色在自己的固定任务宣布入场、范围和停止门。`00 包工头`负责监督与汇总，但不得代替对应角色报到或交付。
13. 无论项目数量、类型或技术栈如何，每个项目都必须有 `ui/` 目录，用于统一保存 UI/UX 提示词、设计说明、原型、生成界面和视觉审核产物；新 UI 交付必须登记权威路径与哈希，已在审核中的旧路径不得为整理目录而中途移动。
14. AIWorkFlow 下每个现在和未来项目的用户可见前端都必须至少提供一套完整、可实际使用的简体中文版；只有英文或其他语言的界面不得视为前端完成。中文版必须覆盖导航、操作、状态反馈、图表标签、错误与空状态、无障碍文案和移动端；项目可以同时支持其他语言，但不得以其替代中文版。
15. 超级无敌帅超超总可在任一固定角色本人任务中直接下达明确项目、范围和动作的临时指令；该指令是本角色该范围的一次性入场或继续授权，无需 `00 包工头`转发，并具有 AIWorkFlow 内部最高业务调度优先级。但不得自动批准待审产物、解冻其他工作、授权下游或跳过原流程；角色必须记录原状态、安全恢复点和返回点，完成后回原队列或审核门。跨角色或冲突冻结现场时直接路由正确固定角色，不得越权代做；高风险动作仍须本次具体授权。
