# AI Workflow Control Center 前端演示纵切测试报告

## 1. 交付元数据

| 项目 | 内容 |
|---|---|
| `project_id` | `workflow-control-center` |
| `change_id` | `test-20260816-control-center-frontend-demo-vertical-slice-001` |
| 产物 | `artifact-control-center-frontend-test-report-001` |
| 负责角色 | 固定 `10 测试工程师`（`role-qa`） |
| 入场授权 | `approval-20260816-control-center-frontend-qa-entry` |
| 上游批准 | `approval-20260816-control-center-frontend-code-rereview-conclusion` |
| 输入产物 | `artifact-control-center-frontend-code-rereview-001` |
| 输入报告 | `docs/06-code-review.md` |
| 输入报告 SHA-256 | `65606bd82da7948a02940cc35b0051193ebd32cc25a2c5872db1e1eb3d558adf` |
| 被测源码提交 | `945b98320002d50605943697c5ce5c5e63ec2800` |
| 测试工作流安全基线 | `368c3cadb35e12020586e62643e6a95f39d93c87` |
| 测试日期 | 2026-08-16（Asia/Shanghai） |
| 报告状态 | 待超级无敌帅超超总审核 |
| 停止门 | `test-report-review` |

## 2. 测试结论

**结论：授权的前端演示纵切通过测试；13/13 个范围验收项通过，9/9 个可执行自动化门禁最终通过，未发现新的 Blocker、Major 或 Minor 缺陷，本轮必须修复项为 0。**

这个“通过”只适用于当前硬编码演示前端的功能、真实性标识、响应式、键盘、Chrome Accessibility Tree、客户端预算与回归，不表示 AI Workflow Control Center 已成为真实监管系统。

**上线建议：不可以上线为真实监管产品，也不得进入生产发布。** 当前页面可以继续作为明确标注的本地／私有演示前端接受审核；生产发布仍冻结，测试报告交付后停在 `test-report-review`，不自动路由 DevOps 或任何后续角色。

## 3. 强制真实性边界

以下事实是本报告结论的一部分，不是可省略的备注：

1. 当前 `frontend_data` 仍是 `hardcoded demo/pending`，页面数字与状态不是当前根仓实时事实。
2. 真实 workflow backend 尚未实现。
3. workflow reader 尚未实现。
4. 数据库状态聚合尚未实现；`db/schema.ts` 仍为空结构，现有前端没有真实监管数据接入。
5. 真实项目、审批、产物、事件、问题和发布数据接入尚未实现。
6. 本轮没有把未实现后端当作已实现进行测试，也没有访问或修改生产环境。
7. 演示测试通过不得解释为真实监管链路完成、Control Center 已真实可用、已具备生产健康度或已获发布授权。
8. 已批准的 `artifact-control-real-usable-product-delta-001` 仍等待唯一重规划工作项；本测试不审批、不实现也不解冻该增量。

## 4. 测试范围与明确排除

### 4.1 本轮覆盖

- 六个一级视图：总览、项目与阶段、角色协作、质量与复测、迭代与发布、成熟度与治理。
- 四维全局筛选：项目、时间范围、迭代、数据来源，包括 URL 恢复、非法值回退、默认值恢复与覆盖不足停显。
- 项目页局部筛选：项目状态、阶段责任角色、数据来源，以及卡片、矩阵和详情一致性。
- 全局搜索、搜索范围隔离、搜索结果定位。
- 筛选一致导出与覆盖不足时禁止导出。
- AI English Learning 演示样本在总览、项目、质量和发布视图的归属一致性；无证据视图与其他项目不得复用该样本。
- Prompt v1.1 的只读边界、演示／待接入标识和无虚假写入反馈。
- 完整简体中文可用性：导航、筛选、操作、状态、错误／空状态、无障碍名称和移动端主要内容；产品名、角色缩写与行业严重度术语保留原名。
- 1440×1000、1024×768、390×844、320×720 响应式。
- 键盘操作、焦点进入与恢复、原生表格／按钮语义、`aria-pressed`、`disabled` 和 Chrome AX tree。
- 客户端块预算、六视图懒加载、构建／静态／领域／浏览器回归。

### 4.2 本轮排除

- 把真实 workflow backend、workflow reader、数据库聚合或真实数据接入当作已实现进行测试。
- 修改业务代码、产品需求、UI/UX、部署配置或生产资源。
- 真实生产 URL 的发布验收、响应头、安全审计、真实网络数据时序和生产性能。
- 批准本测试报告、批准未来产物、自动路由 DevOps 或启动后续角色。

## 5. 环境、基线与独立性

| 项目 | 结果 |
|---|---|
| 根仓 | `/Users/qichao/Desktop/DEV/Lenovo/AIWorkFlow`，单一 Git 根 |
| 测试起始 `HEAD/origin/main` | `368c3cadb35e12020586e62643e6a95f39d93c87`，一致 |
| 被测源码 | `945b98320002d50605943697c5ce5c5e63ec2800` |
| 源码漂移 | 从被测提交到测试基线，`control-center/app`、`tests`、包与前端配置无漂移 |
| 输入完整性 | `docs/06-code-review.md` SHA-256 精确匹配 |
| Node.js | `24.19.0`，满足项目 `>=22.13.0` |
| 浏览器 | Google Chrome `151.0.7922.138` |
| 操作系统 | macOS `15.7.4`，arm64 |
| 浏览器服务 | 本地隔离生产构建服务；未使用生产站点作为通过证据 |
| 代码改动 | 0；测试期间未修改业务代码、产品或 UI |

本报告没有照抄代码复审结论：QA 重新运行 lint、类型检查、构建、静态／领域／性能／真实 Chrome 门禁，并使用 Playwright CLI 对本地生产构建的总览、质量页和 320×720 可访问性快照进行独立抽查。

## 6. 范围验收结果

| ID | 验收项 | 独立证据 | 结果 |
|---|---|---|---|
| QA-CC-01 | 输入、源码与 Git 基线完整 | 报告哈希匹配；被测提交为安全基线祖先；目标源码无漂移 | 通过 |
| QA-CC-02 | 六个一级视图独立可用 | 6 个视图均有独立动态入口并在真实 Chrome 渲染 | 通过 |
| QA-CC-03 | 四维全局筛选与 URL 状态 | 四类值完整恢复；非法值回默认；待接入／7 天／其他迭代在六视图诚实停显 | 通过 |
| QA-CC-04 | 项目局部筛选一致性 | 状态、责任角色、来源筛选与卡片、矩阵、详情共用同一结果集 | 通过 |
| QA-CC-05 | 搜索范围与定位 | 隐藏项目不泄漏；待接入来源无演示结果；English 结果定位并同步项目筛选 | 通过 |
| QA-CC-06 | 筛选一致导出 | English 单项目导出只含 English；覆盖不可用时不创建对象 URL、不触发下载 | 通过 |
| QA-CC-07 | English 样本跨视图一致性 | 总览、项目、质量、发布使用同一项目归属；角色／治理无项目证据时停显；Model 不复用 English 明细 | 通过 |
| QA-CC-08 | Prompt v1.1 只读与真实性 | 质量页写操作为原生禁用；治理样例无可执行按钮；持续显示演示／非实时／待接入 | 通过 |
| QA-CC-09 | 完整简体中文可用性 | `lang=zh-CN`；导航、操作、状态、筛选、空／错提示、AX 名称与移动端主要内容为简体中文 | 通过 |
| QA-CC-10 | 响应式 | 六视图 × 1440／1024／390／320，共 24/24 组合无页面级横向溢出 | 通过 |
| QA-CC-11 | 键盘与焦点 | Enter 打开详情、Escape 关闭、焦点恢复；角色按钮键盘选择保持单选状态 | 通过 |
| QA-CC-12 | Chrome AX／读屏结构代理 | 筛选名称、原生表格、列／行标题、按钮、`pressed`、`disabled` 均进入 AX tree；Playwright 快照复核中文名称 | 通过 |
| QA-CC-13 | 客户端预算与回归 | 最大块 271,360 B；Dashboard 壳 90,117 B；六视图均懒加载；浏览器控制台 0 error / 0 warning | 通过 |

范围验收通过率：**13/13（100%）**。

## 7. 自动化与浏览器执行记录

| 检查 | 结果 |
|---|---|
| `npm run lint` | 通过，0 error / 0 warning |
| `npm run typecheck` | 通过 |
| 生产构建 | 通过 |
| 静态测试 | 3/3 通过 |
| 领域测试 | 3/3 通过 |
| 客户端预算测试 | 2/2 通过 |
| 真实 Chrome 门禁 | 1/1 通过 |
| 自动化合计 | 9/9 通过 |
| 浏览器视图／视口组合 | 24/24 通过 |
| 浏览器控制台 | 0 error / 0 warning |
| 最大客户端 JavaScript 块 | 271,360 B，低于 500,000 B 门禁 |
| Dashboard 壳块 | 90,117 B，低于 180,000 B 门禁 |
| 六视图动态入口 | 6/6 通过 |

执行期间的首个 `npm test` 在进入浏览器门禁时被受限沙箱以 `listen EPERM 127.0.0.1` 阻止；此前构建与 8 项非浏览器测试均已通过。随后在获准的本机临时端口独立重跑 `npm run test:browser`，真实 Chrome 门禁 1/1 通过。因此该事件判定为测试环境阻塞，不是产品缺陷，也没有被隐藏为首次即通过。

一次开发模式 Playwright 抽查因开发隧道命中了其他本地页面而被判为无效证据并丢弃；随后改用隔离的 Vinext 生产构建服务 `127.0.0.1:4317` 重新取证，页面标题、URL、质量页只读控件和 320×720 AX 快照均正确。临时浏览器、服务与快照已关闭并清理。

## 8. 缺陷与遗留处置

| 分类 | 数量 | 处置 |
|---|---:|---|
| Blocker / P0 | 0 | 无 |
| Major / P1 | 0 | 无 |
| Minor / P2 | 0 | 无 |
| 本轮必须修复 | 0 | 无 |
| 已明确接受为遗留缺陷 | 0 | 无需以遗留缺陷放行 |

真实性缺口不是本轮演示前端的新 Bug，但它们阻断“真实可用”和生产上线：真实 workflow backend、workflow reader、数据库状态聚合、真实数据接入均未实现。它们必须在已批准产品增量的后续唯一重规划工作项中独立规划、实现、审查和测试，不能由本报告豁免。

## 9. 已知限制与剩余风险

1. 本轮用 Chrome AX tree 和 Playwright 可访问性快照验证读屏结构，没有执行 VoiceOver／NVDA 的实际语音输出会话，因此不扩大为“所有辅助技术完全兼容”。
2. 未测试生产 URL、CSP/HSTS 等响应头、真实网络交互时序、真实后端异常、数据库一致性或生产负载。
3. 未重新执行联网依赖漏洞审计；上游记录的 npm 镜像 audit endpoint 404 仍是独立非阻断限制，不能解释为“依赖无已知漏洞”。
4. 页面 5 项缺陷、门禁和成熟度数字都是带明确标签的演示内容，不是本轮发现的真实项目缺陷或真实治理结论。

## 10. QA Checklist

- [x] 授权范围和上游输入已核验。
- [x] 正常、边界、非法 URL、覆盖不足和空结果场景已覆盖。
- [x] 六视图、全局／局部筛选、搜索、导出和跨视图一致性已覆盖。
- [x] 1440、1024、390、320 已覆盖。
- [x] 键盘、焦点和 Chrome AX 已覆盖。
- [x] 核心回归和客户端预算有可执行门禁。
- [x] 本轮没有未关闭 Blocker／Major／Minor。
- [x] 未关闭缺陷已互斥分类；本轮必须修复 0，接受遗留 0。
- [x] demo/pending、后端缺失和生产冻结已写入结论。
- [x] 未修改业务代码，未进入部署或生产。

## 11. 停止门与审核选项

- 当前产物：`artifact-control-center-frontend-test-report-001`
- 当前停止门：`test-report-review`
- QA 建议：**批准“前端演示纵切测试通过”的结论，但保持真实产品不可上线、生产发布冻结。**
- 本轮没有本轮必须修复缺陷；同时，最新真实可用产品增量仍待唯一重规划工作项，且本次授权明确禁止自动路由 DevOps 或后续角色。因此本报告审核不触发下一角色。
- 审核选项：
  1. `通过`：只批准本测试报告与当前演示纵切测试结论，返回既有跨项目重规划队列；不授权部署或生产发布。
  2. `修改`：指出需要调整的测试范围、证据或结论，由固定 `10 测试工程师`修订后重新交付。
  3. `继续测试`：明确新增的测试范围；未明确范围前不扩大测试。
