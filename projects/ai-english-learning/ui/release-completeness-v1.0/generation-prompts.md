# EL-UI-002 内置 ImageGen 生成记录

生成方式：内置 `image_gen.imagegen`，参考图引导模式。  
生成日期：2026-08-16（Asia/Shanghai）  
权威边界：13 项用户 UI 母版全部构成视觉输入；受工具每次最多 5 个参考文件限制，每张稿选择与该页面最相关的 5 项，未选入单次调用的母版仍通过整体设计说明约束视觉一致性。  
后处理：复制内置生成原件到项目新版本目录，使用 `sips` 无裁切归一到目标像素；原件保留，未删除。

## 1. 1440 桌面首页与入口真相

参考：总览、桌面首页、桌面单词、桌面对话、色板。

```text
为“AI English Learning”生成一张高保真 Web 产品桌面端 UI 视觉稿，严格继承参考图的暖米色背景、暖白卡片、青绿色与品牌紫、深紫文字、圆角、轻阴影和留白；降低旧参考的发光和过重阴影。纯网页截图，无设备外框、海报说明或漂浮装饰。目标逻辑视口 1440×1024，完整简体中文。

页面为“今日学习”：左侧品牌紫导航“首页、背单词、练口语、统计、设置”；标题下真相条“目标态演示 · 当前真实服务未全部接通”，并显示“最近成功刷新：尚无记录”。主区只突出“背单词”“练口语”两张行动卡。下方为“今日计划：尚未生成真实计划 / 等待定级与词库就绪”“待复习：12（演示数据），今日到期 8 / 逾期 4”“能力状态：AI 对话尚未就绪、语音识别降级为文字、语音播放尚未就绪”。右栏显示“访客模式 / 跨设备同步不可用 / 登录后同步”和独立正确率口径。状态同时使用文字与图标，不伪装已接通。
```

## 2. 1024 单词作答与跨端冲突

参考：总览、桌面单词、移动单词、桌面设置 / 统计、色板。

```text
生成高保真 1024×768 平板横屏 Web UI，继承暖米色、暖白、青绿、品牌紫、圆角和轻阴影。页面标题“单词复习”，真相条“目标态演示 · 当前服务端学习记录尚未接通”。左 65% 为句中唯一行内字母槽：“Please r e v _ _ _ your email before sending.”，中文释义，状态“保存尚未确认 · 不会计分或推进阶段”，操作“检查答案、提示、查看答案、跳过本题、暂停此词”。右 35% 为“同步冲突”：设备 A 已查看答案、设备 B S2 作答中，合并结果保留查看证据且阶段不重复推进；再显示复习证据时间线 D0 → D+1 → D+3（演示数据）和写入来源、规则版本、尚无成功记录。不要句下独立答案框，不显示已掌握或已同步成功。
```

## 3. 390 真实 AI / STT / TTS 降级

参考：总览、移动对话、桌面对话、移动首页、色板。

```text
生成 390×844 竖屏移动 Web UI，纯网页截图，继承权威母版。页面“自由对话”，真相条“目标态演示 · AI 与语音服务当前未接通”，状态“文字输入可用 · 麦克风未授权 · 语音播放尚未就绪”。保留已确认英文对话历史；最后显示“AI 回复未完成”，说明没有用缓存脚本替代新回复、输入和历史已保留，提供“重试回复 / 稍后再试”。底部文字输入始终可用；麦克风正确禁用并可查看原因；说明识别失败可改文字、原始音频默认不长期保存。触控不小于 44px，不伪造波形、播放成功或发音分数。
```

## 4. 320 双导出与删除未就绪

参考：总览、移动设置、桌面设置、移动首页、色板。

```text
重做“账号与数据”小屏页面，保持相同产品语义和视觉语言，重点修正 320×568 实际像素下的可读性。生成高分辨率竖屏后会归一到 320×568，因此原图正文至少 48px、辅助至少 42px、标题至少 66px、按钮文字至少 48px；使用更短文案腾出空间。页面含“目标态演示 · 数据服务未接通”“学习者账号 / 保存尚未确认 / 最近同步：尚无记录”“下载可阅读报告 / 下载机器可读数据 / 尚未就绪，不生成空文件”“删除账号与数据 / 登录立即失效 / 活动数据目标 24 小时 / 备份最长 30 天”“确认删除？ / 服务未就绪，当前不可删除 / 删除服务未就绪 / 取消”“数据说明”。所有内容单列，按钮最终至少 44px 高，禁用态仍可读，不显示成功。
```

## 5. 生成质量边界

- PNG 是视觉状态样例，页面文案与交互权威以 `ui/06-release-completeness-ui-design-v1.0.md` 为准。
- 图中“目标态演示”“演示数据”不代表功能已接通。
- 内置生成件已由固定 04 逐张目检尺寸、构图、主文案、状态真相和边界；尚未经过独立视觉审查。
