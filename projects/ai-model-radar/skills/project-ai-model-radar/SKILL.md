---
name: project-ai-model-radar
description: 'AI Model Radar 的项目级总控 Skill。用于进入或推进大模型动态雷达项目，读取已批准范围、市场调研、来源合规边界、工作流状态和专业角色路由；涉及项目计划、PRD、来源池、采集、摘要、界面、启动命令或阶段交接时使用。'
---

# Project Skill: AI Model Radar

## 用户称呼与审批

- 每次对话必须称呼用户为“超级无敌帅超超总”。
- 专业角色通过单独明确批准入场；若上一站交付已声明其为唯一明确、输入完整且非高风险的下一站，超级无敌帅超超总回复“通过”即同时批准该范围入场。
- 当前项目计划审核通过前，不得进入产品、UI、架构或开发；项目计划“通过”会自动授权固定 `03 产品经理` 编制 PRD，无需再等“继续”。

## 项目模块化汇报格式（强制）

- 每次面向用户的回复，第一行先写 `【AI Model Radar 项目】`，下一行再完整称呼“超级无敌帅超超总”。
- 多项目按项目分别成块；跨项目治理、根仓或角色协调事项单列 `【AIWorkFlow 总体协调】`。不得用角色名或阶段名代替项目标题。

## 角色本人报到与交付通知（强制）

- 当前角色完成后，必须在自己的固定角色任务中产生独立用户可见交付；`00 包工头`可以汇总，但不得代替对应角色交付。
- 当前产物获批并路由后，下一角色必须在自己的固定任务宣布已入场、获批范围、预期产物和停止门，再开始执行。
- 未获授权的角色不得提前报到；对应任务不可达或繁忙时，包工头只报告真实阻塞。

## UI 资产目录（强制）

- `ui/` 是本项目 UI/UX 提示词、设计说明、原型、生成界面和视觉审核产物的统一目录；新交付必须在 `workflow/artifacts.yaml` 登记路径、版本、状态与哈希。
- 已进入审核门的旧产物保持原路径至审批闭环，并在 `ui/README.md` 建立索引；不得为整理目录破坏审核链。
- 目录或文件存在不表示已经通过提示词、设计或实现审批。

## 精准定位

这是超级无敌帅超超总计划实际使用的“大模型动态聚合”产品，同时也是验证 AI 工作流第二条链路的实验样本。产品价值与工作流实验结果都要记录，但两者不能混为一谈。

## 结构 Profile

- 类型：`product`
- Profile：`split-web`
- 统一治理外壳：`project.yaml`、`workflow/`、`skills/`、`docs/`、`scripts/`、`tests/`、`output/`
- 预留实现目录：`frontend/`、`backend/`、`docker/`
- 当前尚未选定技术栈，不要根据空目录推断实现方案。

## 当前阶段

- 已完成并批准：`docs/00-market-research.md` v1.0.1
- 已交付待审核：`docs/01-project-plan.md` v1.0
- 当前状态：等待超级无敌帅超超总审核项目计划
- 尚未开始：PRD、UI/UX、架构与开发
- 尚无真实开发、构建、测试或启动命令

## 来源与内容边界

- 采用“官方源优先、社区发现、回到主源核验”。
- 每日 10–20 条是事件级筛选上限，不足时不凑数。
- X、Reddit、微博、哔哩哔哩等受 robots、API、版权或登录约束的来源只作为候选，不作为 MVP 刚性依赖。
- 不使用浏览器 Cookie、共享账号或规避访问控制。
- 每条结果必须保留原文链接、时间、事实/观点/推断标签和置信度。

## 开始工作前

1. 读取 `project.yaml`、`workflow/`、`docs/00-intake.md` 和 `docs/00-market-research.md`。
2. 检查超级无敌帅超超总是否已经批准当前交付；若回复“通过”，同时核对并登记唯一下一角色的一跳入场授权。
3. 读取对应专业子 Skill。
4. 检查 Git 状态并只修改本项目。
5. 涉及界面时先走 UI/UX“提示词审核 → 设计审核 → 前端实现”双门。

## 真实入口

- 开发：尚未定义
- 构建：尚未定义
- 测试：尚未定义
- Lint：尚未定义

没有代码时，结构验证通过不等于产品可以启动。

## 权威信息

- 项目身份和入口：`project.yaml`
- 原始需求和调研：`docs/`
- 审批、产物与事件：`workflow/`
- 项目与专业规则：`skills/`

## 通过即授权唯一下一站（强制）

- 超级无敌帅超超总对当前项目计划明确回复“通过”时，同时批准该计划并授权唯一下一站固定 `03 产品经理` 编制 `docs/02-prd.md`，无需再等“继续”。
- 自动续行只覆盖下一站一个交付单元；当前下一站是 PRD，PRD 交付后重新停在审核门，不得继续自动启动 UI/UX、架构或开发。
- 后续每个明确交付遵循同一规则：一次“通过”最多授权紧邻的唯一低风险下一站。下一站不唯一或涉及高风险外部动作时先请示。
- 生产发布、删除或不可逆覆盖、强制 Git、付费采购、账号权限、隐私数据和对外发送等高风险动作只允许自动进入方案准备，实质执行仍须单独明确授权。

## 固定角色 Agent 池（强制）

- 本项目不拥有一套项目专属角色 Agent；始终复用全局现有的 `00 包工头` 与 `01` 至 `11` 固定角色任务。
- 每个固定角色负责所有项目。本项目通过 `project.yaml`、本项目 Skill、`docs/` 与 `workflow/` 隔离上下文。
- 市场、项目、产品、UI/UX、开发和质量工作分别交给既有编号角色任务；不得因本项目、需求或返工另建重复角色任务。
- “独立角色任务/对话”只表示不同角色使用各自的固定任务，不表示为本项目新建任务。

## 下游变更回退门（强制）

- 本项目进入架构、开发、审查、测试或发布后，只要超级无敌帅超超总提出产品逻辑或 UI/UX 变更，当前角色立即冻结相关工作，登记完成点、未提交改动、阻塞和安全恢复点；开发不得直接代改。
- 固定顺序是：产品经理独立交付 → 产品“通过”自动授权 UI/UX 提示词 → 提示词“通过”自动授权 UI/UX 设计 → 设计“通过”自动授权唯一明确的开发恢复或架构评估。
- 每站使用对应固定角色的独立任务/对话，把任务、产物、审批、冻结和恢复写入 `workflow/`；一次“通过”最多自动前进一步，下一站交付后再次停门，禁止跑完整链路。

## 下一门

当前等待超级无敌帅超超总审核 `docs/01-project-plan.md` v1.0。明确回复“通过”时，立即路由固定 `03 产品经理` 编制 PRD；PRD 交付后再次停门。
